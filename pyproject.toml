[project]
name = "hack-dbab4d08-ai-job-hunter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pandas>=3.0.6",
]

[project.scripts]
hack-dbab4d08-ai-job-hunter = "hack_dbab4d08_ai_job_hunter:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
